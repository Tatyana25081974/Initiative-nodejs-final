description: Unauthorized access - access token expired or session not found
content:
  application/json:
    schema:
      $ref: ../../schemas/recipes/errors/unauthorized.error.yaml
    examples:
      accessTokenExpired:
        summary: Access token expired
        $ref: ../recipes/examples/accessTokenExpired.recipes.example.yaml
      sessionNotFound:
        summary: Session not found
        $ref: ../recipes/examples/sessionNotFound.recipes.example.yaml
