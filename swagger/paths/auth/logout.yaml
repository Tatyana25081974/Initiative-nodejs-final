tags:
  - Auth
summary: Logout user
operationId: logoutUser
description: Ends the user session. Requires valid access token.
security:
  - bearerAuth: []

responses:
  '204':
    description: Logout successful. Token invalidated or ignored on client side.
  '401':
    $ref: '../../components/responses/401.yaml'
  '500':
    $ref: '../../components/responses/500.yaml'
